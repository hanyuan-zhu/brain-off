# Kimi Agent - CAD 图纸分析系统

## 🎯 核心创新

实现了一个**主动式 AI Agent**，让 Kimi 2.5 能够：
1. **自主决策**：自己思考需要"看"哪些区域
2. **主动调用工具**：按需获取结构化数据和渲染图片
3. **双模态融合**：结合 ezdxf 几何数据 + 视觉 AI 分析
4. **生成专业报告**：输出符合工程标准的分析结果

---

## 📐 系统架构

```
用户任务
   ↓
Kimi Agent (主控)
   ↓
┌─────────────────────────────────────┐
│  工具集 (Agent 主动调用)            │
├─────────────────────────────────────┤
│ 1. get_cad_metadata                 │
│    └─ 提取图层、实体统计            │
│                                     │
│ 2. get_cad_regions                  │
│    └─ 识别高密度区域 (145个)        │
│                                     │
│ 3. extract_cad_entities             │
│    └─ 提取 TEXT/DIMENSION 等        │
│                                     │
│ 4. render_cad_region                │
│    └─ 按需渲染指定坐标区域          │
└─────────────────────────────────────┘
   ↓
结构化数据 + 视觉分析
   ↓
专业分析报告
```

---

## 🔧 核心组件

### 1. 工具定义 ([services/kimi_agent_tools.py](services/kimi_agent_tools.py))

提供 4 个核心工具：

| 工具名称 | 功能 | 输入 | 输出 |
|---------|------|------|------|
| `get_cad_metadata` | 获取文件元数据 | file_path | 图层、实体统计、文件信息 |
| `get_cad_regions` | 识别关键区域 | file_path, grid_size | 高密度区域列表 |
| `render_cad_region` | 渲染指定区域 | file_path, x, y, width, height | PNG 图片路径 |
| `extract_cad_entities` | 提取实体数据 | file_path, entity_types, layers | 实体列表 |

